GEMINI_API_KEY=
VLM_PROVIDER=opencode
VLM_MODEL=gemini-2.5-flash
OPENCODE_API_KEY=
OPENCODE_MODEL=deepseek-v4.1-flash
# Paths are resolved from the repository root by default.
# DATA_DIR=/absolute/path/to/private/runtime
# Guide code (4+ characters) that lets a guide's phone teach and publish routes.
# Without it, teaching and publishing work only on the laptop itself.
# TEACH_PIN=
